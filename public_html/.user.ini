; Read automatically by PHP-FPM (which Hostinger and most shared hosts use) —
; raises the upload ceiling so full-resolution photography uploads through the
; admin CMS aren't silently truncated by PHP's stock 2MB default.
upload_max_filesize = 16M
post_max_size = 20M
max_execution_time = 60
